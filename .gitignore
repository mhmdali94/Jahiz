node_modules/
dist/

# Throwaway Arabic-rendering validation script + its output — see Step 2
# of HOW TO WORK. Not part of the deliverable tree.
scratch/

# CLI secrets — the signing key and issued-serial ledger never get committed
cli/serials.json
cli/*.key
cli/private-key*

# Bulk-issue exports (working files for handing codes out/tracking them —
# not app config, shouldn't live in git)
cli/bulk-issued-*.csv

.DS_Store
*.log
